$currentServer = $servers[0]
switch ($currentServer) {
    servers[0]{
        # Checks to see if SVR1 is online and get content if it is.
        break
    }
    servers[1]{
        # Does the same for SVR2
        break
    }
    servers[2]{
        # Does the same for SVR3
        break
    }
}